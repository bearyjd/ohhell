name: ohhell_protocol
description: Shared wire types for Oh Hell WebSocket protocol.
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.9.2

dependencies:
  ohhell_engine:
    path: ../ohhell_engine

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.6
